#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv){
    if (argc!=4) return 1;
    int n1=argv(1);
    int n2=argv(3);
    char op=argv(2);

    switch (op){
        case '+':
        printf("%d\n", (n1+n2));
        break;

        case ''
    }
}
